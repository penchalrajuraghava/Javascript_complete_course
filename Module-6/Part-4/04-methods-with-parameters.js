/*
=========================================
Methods with Parameters
=========================================
*/

const rectangle = {
  area(length, width) {
    return length * width;
  },
};

console.log(rectangle.area(20, 10));
