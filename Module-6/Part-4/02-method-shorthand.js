/*
=========================================
Method Shorthand
=========================================
*/

const calculator = {
  add(a, b) {
    return a + b;
  },

  subtract(a, b) {
    return a - b;
  },
};

console.log(calculator.add(20, 10));
console.log(calculator.subtract(20, 10));
