/*
=========================================
Object.getPrototypeOf()
=========================================
*/

const mobile = {
  brand: "OnePlus",
};

const prototype = Object.getPrototypeOf(mobile);

console.log(prototype);
